<Page x:Class="KuFi.UI.Views.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="LogsPage"
      Background="{DynamicResource BgDeepDarkBlue}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="60,30,60,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="LOGS   >   " Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold"/>
                <TextBlock Text="SECURITY ACTIVITY" Foreground="{DynamicResource EthericMint}" FontSize="12" FontWeight="Bold"/>
            </StackPanel>
            <TextBlock Text="Security Logs &amp; Activity" Foreground="{DynamicResource TextPrimary}" FontSize="32" FontWeight="SemiBold"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource CardBackground}" CornerRadius="16" Padding="30">
            <TextBlock Text="Log activity will be displayed here in the next update." Foreground="{DynamicResource TextSecondary}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Page>
